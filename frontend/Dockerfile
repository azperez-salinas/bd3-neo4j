# Frontend estático mínimo (HTML/CSS/JS plano), listo como base para sumar pantallas
FROM nginx:alpine

# Sirve los archivos estáticos del proyecto directo con Nginx, sin paso de build
COPY . /usr/share/nginx/html

EXPOSE 80
